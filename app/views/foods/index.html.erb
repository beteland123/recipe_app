<p>index page</p>
<div class='sign_in_user_bar'>
      <% unless user_signed_in? %>
        <div class="user-sign-in-link">
          <%= link_to 'Sign in', new_user_session_path, method: :delete %>
        </div>
      <% end %>
      <% if user_signed_in? %>
        <div class="user-sign-in-link">
          <%= link_to 'Sign out', custom_destroy_user_session_path, method: :delete %>
        </div>
      <% end %>
</div>